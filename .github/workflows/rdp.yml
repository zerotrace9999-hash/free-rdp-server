name: RDP

on: [workflow_dispatch]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Enable RDP and Create User
        run: |
          # 1. إنشاء حساب المسؤول
          net user kamel123 MyStrong123 /add
          net localgroup administrators kamel123 /add
          
          # 2. تفعيل خدمة سطح المكتب عن بعد وفتح جدار الحماية
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name 'fDenyTSConnections' -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"

      - name: Start Free Tunnel via Pinggy
        run: |
          # فتح النفق باستخدام العنوان المخصص للـ TCP مع خيارات الاستقرار
          ssh -T -N -o StrictHostKeyChecking=no -R 0:localhost:3389 rdp@ran.pinggy.io
